CREATE DATABASE APS_DSW;

CREATE TABLE DISCIPLINA (
    ID_DISCIPLINA INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    NOME_DISCIPLINA VARCHAR(50) NOT NULL
);

ALTER TABLE DISCIPLINA AUTO_INCREMENT=1111;

INSERT INTO DISCIPLINA (NOME_DISCIPLINA) VALUES ('Desenvolvimento De Software Web');
INSERT INTO DISCIPLINA (NOME_DISCIPLINA) VALUES ('Analise E Complexidade De Algoritmos');
INSERT INTO DISCIPLINA (NOME_DISCIPLINA) VALUES ('Laboratório de Redes');
INSERT INTO DISCIPLINA (NOME_DISCIPLINA) VALUES ('Banco de Dados II');
INSERT INTO DISCIPLINA (NOME_DISCIPLINA) VALUES ('Engenharia de Software II');

CREATE TABLE ALUNO (
    ID_ALUNO INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
    NOME_ALUNO VARCHAR(50) NOT NULL,
	CPF_ALUNO DECIMAL(11,0) NOT NULL,
    CEP NUMERIC NOT NULL,
    RUA VARCHAR(50) NOT NULL,
    NUMERO INTEGER NOT NULL,
    BAIRRO VARCHAR(50) NOT NULL,
    CIDADE VARCHAR(50) NOT NULL,
    ESTADO VARCHAR(50) NOT NULL,
    EMAIL VARCHAR(50) NOT NULL,
    TELEFONE DECIMAL(11,0) NOT NULL,
	UNIQUE (CPF_ALUNO)
);

ALTER TABLE ALUNO AUTO_INCREMENT=222222;

CREATE TABLE MATRICULA(
    ID_MATRICULA INT PRIMARY KEY AUTO_INCREMENT,
    AVALIACAO_1 FLOAT,
    AVALIACAO_2 FLOAT,
    AVALIACAO_3 FLOAT,
    MEDIA FLOAT,
    CONCEITO CHAR(1),
	DISCIPLINA_ID INT NOT NULL,
    ALUNO_ID INT NOT NULL,
    FOREIGN KEY (DISCIPLINA_ID) REFERENCES DISCIPLINA(ID_DISCIPLINA),
    FOREIGN KEY (ALUNO_ID) REFERENCES ALUNO(ID_ALUNO)
);